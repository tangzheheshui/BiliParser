# 需求：试用 3 天 + 激活（授权模型改造）——**已废弃**

> 状态：**已废弃（2026-08-25）**。v2 需求重构（[服务器需求文档.md](服务器需求文档.md) /
> [客户端需求文档.md](客户端需求文档.md)）删除了试用体系：无试用、无 AI 代理、
> 无每日配额，AI 改为用户自有 key。本文只作历史决策记录保留，**不反映现状**。
> 相关代码（`/api/trial/register`、`trials` 表、试用 UI）已随 v2 重构移除。

以下为废弃前的记录，仅供追溯：

## 一、目标模型（已废弃）

```
首次启动 ──► 免激活，直接进工作台（试用 3 天）
          3 天到期 ──► 锁 AI 生成，弹激活窗 ──► 一码一机
```

## 二、核心决策（2026-08-22 拍板）

1. 试用状态存服务器端（指纹 → first_seen_at），删本地文件无法重置
2. 试用期 AI 服务器代理、卖家垫钱 + 每日限额
3. 72 小时精确起算，服务器时间
4. 到期只锁 AI 生成

## 三、为什么废弃

用户 2026-08-25 重写需求：v1 模型（服务器代理 AI + 配额 + 试用 + 网页版托管）
太复杂、售后面太宽。v2 收敛为「激活码库存池 + 一次性激活 + 本地验签 +
用户自有 AI key」，卖家日常只剩发码一件事。网页版 hosted.py 也一并删除
（问题多、不再维护）。
